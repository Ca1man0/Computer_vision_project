# ---------------------------------------------------------------------------
# Minecraft Mob Detection - Python dependencies
# Tested on Google Colab (Python 3.10, Tesla T4 GPU)
# Install with:  pip install -r requirements.txt
# ---------------------------------------------------------------------------

# --- Core scientific stack ---
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12

# --- Computer vision ---
opencv-python>=4.8          # cv2: image I/O, color spaces, filters, HOGDescriptor
Pillow>=9.5                 # image handling

# --- Classical ML pipeline (HOG + SVM, k-NN) ---
scikit-learn>=1.3           # SVM, k-NN, StandardScaler, metrics, train_test_split

# --- Deep learning pipeline (YOLOv5-nano) ---
torch>=2.0                  # PyTorch (use the CUDA build on GPU machines)
torchvision>=0.15
PyYAML>=6.0                 # reads/writes data.yaml for YOLOv5

# --- Dataset download ---
kagglehub>=0.2              # downloads dracotlw/minecraft-mobs-yolo-dataset

# ---------------------------------------------------------------------------
# NOTE ON YOLOv5
# YOLOv5 is cloned at runtime from https://github.com/ultralytics/yolov5
# and brings its own requirements.txt (installed inside MOD_4). If you run the
# notebooks locally, also run:
#     git clone https://github.com/ultralytics/yolov5
#     pip install -r yolov5/requirements.txt
# ---------------------------------------------------------------------------
